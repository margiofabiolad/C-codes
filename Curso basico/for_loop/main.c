#include <stdio.h>
#include <stdlib.h>

int main()
{
   /* for (init; condition; increment)
   {
   statement(s);
   } */
   for (int i=0; i<10; i++) // the for first reds the variable, after check the condition, print and finally does the sum?
    {
    printf("Value of i = %d \n",i);
    }
}
